#!/usr/bin/expect -f
set timeout 20
set password "dzDnnoI6NMQo1N7g."
spawn ssh -o StrictHostKeyChecking=no root@203.57.85.2 "uname -a; cat /etc/os-release; node -v; pm2 -v; nginx -v"
expect {
    "*?assword:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
